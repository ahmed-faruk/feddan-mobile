#include? "Pods/Target Support Files/Pods-Runner/Pods-Runner.debug.xcconfig"
#include "Generated.xcconfig"
// H1: Local API keys (gitignored). Copy SecretKeys.xcconfig.example → SecretKeys.xcconfig.
#include? "SecretKeys.xcconfig"

// Disable code signing for simulator debug builds — prevents keychain hang
CODE_SIGNING_REQUIRED = NO
CODE_SIGNING_ALLOWED = NO
CODE_SIGN_IDENTITY =
